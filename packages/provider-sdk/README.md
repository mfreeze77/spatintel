# SIP Provider SDK

Stable provider protocols for capture import, registration, and representation conversion. Implementations never publish directly: outputs enter quarantine, retain provenance, pass use-specific quality review, and are atomically bound by the representation publisher.
