# Registration/optimization providers

The approved local reference backend supplies deterministic Sim(3)/SE(3), RANSAC, and coarse-gated ICP. GTSAM and Ceres remain provider interfaces until exact source, ABI, image digest, solver configuration, and license manifests are approved. No backend can silently change source authority or certify a measurement.
