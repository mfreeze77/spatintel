# Progress 07 security policies

These machine-readable policies are deny-by-default inputs to deployment and release controls. They do not authorize production. Provider/model egress, secret material, and release promotion require explicit server-side evidence and exact policy approval.
