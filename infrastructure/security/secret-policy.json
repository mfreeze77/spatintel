{
  "schema": "sip.secret-policy/v1",
  "default": "deny",
  "approved_backends": ["managed-kms", "external-secret-manager", "hardware-backed-key-store"],
  "production_requirements": {
    "repository_material": "prohibited",
    "runtime_injection": "mounted-file-or-workload-identity",
    "rotation": "required",
    "audit": "required"
  },
  "allowed_references": ["vault://", "sip-asset://"],
  "production_authorized": false
}
