schema_version: '1.0'
requirement_id: PLTGRPC-004
priority: P1
source:
  document: 60-platform/602_grpc_worker_api.md
  line: 39
  section: 4. Normative requirements
requirement: The control plane shall verify output hashes and manifest before accepting completion.
verification_method: Automated test
implementation_status: VERIFIED
implementation_files:
- src/sip/worker_protocol.py
- src/sip/worker_runtime.py
- schemas/protobuf/worker.proto
- schemas/jsonschema/worker-lease.schema.json
automated_test_ids:
- tests/contract/test_infrastructure.py::test_compose_workers_have_private_staging_volumes
- tests/contract/test_observability_infrastructure.py::test_workers_publish_metrics_without_broadening_compute_permissions
- tests/contract/test_worker_manifests.py::test_production_worker_requires_exact_deployment_bindings
- tests/contract/test_worker_protocol.py::test_control_plane_rejects_output_hash_mismatch
retained_evidence:
- build/reports/test-matrix.json
- build/reports/tests/contract.xml
controlled_procedure:
- Verify all listed implementation files exist at the reviewed commit.
- Execute each listed automated test or the documented controlled procedure in an isolated environment.
- Retain machine-readable results, input/output hashes, toolchain, code revision, and environment profile.
- Confirm negative/failure behavior and authority, consent, security, recovery, or licensing controls applicable to the requirement.
- Update the ledger only after evidence paths exist and independently match the claimed result.
external_validation:
  status: NOT_REQUIRED
  owner: SIP Engineering
  gap: Verified in the deterministic local reference profile by the exact linked automated test(s) and retained evidence;
    no external hardware, cloud, GPU, legal, or customer-pilot result is inferred.
