schema_version: '1.0'
requirement_id: DATDB-002
priority: P0
source:
  document: 50-data/507_storage_database_schema.md
  line: 37
  section: 4. Normative requirements
requirement: Spatial indexes shall use declared SRID/frame semantics and shall not mix local frames as if they shared one
  CRS.
verification_method: Controlled verification
implementation_status: IMPLEMENTED_UNVERIFIED
implementation_files:
- src/sip/database.py
- migrations
automated_test_ids:
- tests/contract/test_progress06_r1_checkpoint_controls.py::test_progress06_r1_append_only_migration_is_integrity_locked
- tests/contract/test_progress06_r1_checkpoint_verifier.py::test_progress06_r1_scope_predecessor_migration_and_posture_are_exact
- tests/contract/test_progress07_checkpoint_controls.py::test_progress07_append_only_migration_is_integrity_locked
- tests/contract/test_progress08_checkpoint_controls.py::test_progress08_append_only_migration_is_integrity_locked
- tests/contract/test_progress09_checkpoint_controls.py::test_progress09_append_only_migration_is_integrity_locked
retained_evidence:
- build/reports/test-matrix.json
- build/reports/tests/contract.xml
controlled_procedure:
- Verify all listed implementation files exist at the reviewed commit.
- Execute each listed automated test or the documented controlled procedure in an isolated environment.
- Retain machine-readable results, input/output hashes, toolchain, code revision, and environment profile.
- Confirm negative/failure behavior and authority, consent, security, recovery, or licensing controls applicable to the requirement.
- Update the ledger only after evidence paths exist and independently match the claimed result.
external_validation:
  status: NOT_REQUIRED
  owner: SIP Engineering
  gap: Implementation and automated checks exist, but production-environment or full-scope evidence is incomplete; this requirement
    is intentionally not marked verified.
